import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        alignContent: 'center',
        backgroundColor: 'red'
    },
    playButton: {
        width: 65,
        height: 45,
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 15
    }
});